# 🏢 多租户功能配置指南

## 📋 概述

本系统实现了完整的多租户功能，支持：
- ✅ 超级管理员（superadmin）：管理所有租户
- ✅ 租户管理员（tenant_admin）：管理自己租户的用户和资源
- ✅ 租户用户（tenant_user）：访问自己租户的数据

## 🚀 快速配置步骤

### 步骤 1：执行数据库配置脚本

在数据库中执行配置脚本：

```bash
psql -U postgres -d onebase -f scripts/setup_multi_tenant.sql
```

或者在 SQL 查询页面中执行 `scripts/setup_multi_tenant.sql` 文件的内容。

这个脚本会：
1. ✅ 创建 `management` schema
2. ✅ 创建租户相关表（tenants, tenant_databases, tenant_schemas等）
3. ✅ 扩展 users 表，添加 `role` 和 `is_active` 字段
4. ✅ 创建超级管理员账号
5. ✅ 创建示例租户数据

### 步骤 2：编译后端代码

```bash
cd D:\code\onebase
cargo build --release
```

### 步骤 3：重启后端服务

```bash
cargo run --release
```

### 步骤 4：使用超级管理员账号登录

**默认超级管理员账号：**
- 邮箱：`superadmin@example.com`
- 密码：`admin123`

### 步骤 5：访问租户管理页面

登录后，侧边栏会显示 "租户管理" 菜单（仅超级管理员可见）

访问：`http://localhost:3001/dashboard/admin/tenants`

## 🎯 功能说明

### 角色权限对比

| 功能 | superadmin | tenant_admin | tenant_user |
|------|------------|--------------|-------------|
| 创建/管理租户 | ✅ | ❌ | ❌ |
| 查看所有租户 | ✅ | ❌ | ❌ |
| 管理租户用户 | ✅ | ✅（仅自己租户） | ❌ |
| 访问数据库 | ✅ | ✅ | ✅ |
| 切换连接 | ✅ | ✅ | ✅ |
| SQL 查询 | ✅ | ✅ | ✅ |
| ER 图可视化 | ✅ | ✅ | ✅ |

### 超级管理员功能

1. **租户管理**
   - 创建新租户
   - 查看所有租户列表
   - 编辑租户信息
   - 暂停/恢复租户
   - 查看租户详情（用户、连接、Schema）

2. **系统统计**
   - 总租户数
   - 活跃租户数
   - 总用户数
   - 总连接数

3. **用户管理**
   - 为租户创建管理员账号
   - 查看所有用户
   - 管理用户权限

### 租户管理员功能

1. **连接管理**
   - 添加数据库连接
   - 测试连接
   - 切换连接

2. **Schema 管理**
   - 配置业务 Schema
   - 设置 Schema 类型

3. **用户管理**（开发中）
   - 邀请租户成员
   - 分配租户内角色

### 租户用户功能

1. **数据访问**
   - 查看表结构
   - 执行 SQL 查询
   - 导出数据
   - ER 图可视化

2. **连接切换**
   - 在可访问的连接间切换

## 📊 数据库结构

### Management Schema

```
management
├── tenants                    # 租户表
├── tenant_databases           # 租户数据库连接配置
├── tenant_schemas             # 租户业务 Schema
├── user_tenants               # 用户-租户关联
├── connection_access_logs     # 连接访问日志
├── v_tenant_info              # 租户信息视图
└── v_user_connections         # 用户连接视图
```

### Users 表扩展

添加了以下字段：
- `role` - 全局角色（superadmin/tenant_admin/tenant_user）
- `is_active` - 账号是否激活

## 🔧 API 接口

### 超管接口（需要 superadmin 角色）

```
GET    /api/admin/tenants              # 获取所有租户列表
POST   /api/admin/tenants              # 创建新租户
GET    /api/admin/tenants/:id          # 获取租户详情
PATCH  /api/admin/tenants/:id          # 更新租户信息
GET    /api/admin/stats                # 获取系统统计
```

### 租户接口（需要认证）

```
GET    /api/tenants/my-connections           # 获取我的连接
GET    /api/tenants/:tenant_id/schemas       # 获取租户 Schema
POST   /api/tenants/test-connection          # 测试连接
POST   /api/tenants/connections              # 创建连接
POST   /api/tenants/switch-connection        # 切换连接
GET    /api/tenants/pool-stats               # 连接池统计
```

## 🎨 前端页面

### 超管专用页面

- `/dashboard/admin/tenants` - 租户管理
- `/dashboard/admin/tenants/:id` - 租户详情（待实现）
- `/dashboard/admin/users` - 用户管理（待实现）

### 通用页面

- `/dashboard/visualizer` - ER 图可视化
- `/dashboard/connections` - 我的连接
- `/dashboard/data` - 数据浏览
- `/dashboard/sql` - SQL 查询

## 💡 使用示例

### 1. 创建新租户

1. 使用超级管理员登录
2. 访问 "租户管理" 页面
3. 点击 "创建租户"
4. 填写租户信息和管理员账号
5. 提交创建

### 2. 为租户添加数据库连接

1. 使用租户管理员登录
2. 访问 "连接管理" 页面
3. 点击 "添加连接"
4. 填写数据库连接信息
5. 测试连接
6. 保存

### 3. 切换数据库连接

1. 点击侧边栏顶部的连接选择器
2. 选择要切换的连接
3. 系统自动切换，相关页面自动刷新

## 🔒 安全注意事项

1. **密码加密**
   - 数据库连接密码使用简单加密存储
   - 生产环境应使用 AES 或其他强加密算法

2. **权限检查**
   - 所有超管接口都会检查用户角色
   - 租户用户只能访问自己租户的数据

3. **Token 管理**
   - JWT Token 包含用户角色信息
   - 定期刷新 Token

4. **审计日志**
   - 记录所有连接切换操作
   - 记录敏感操作（创建/删除租户）

## 🐛 故障排查

### 问题 1：登录后提示 "无权访问"

**原因：** 用户角色不是 superadmin

**解决：**
```sql
UPDATE users SET role = 'superadmin' WHERE email = 'your@email.com';
```

### 问题 2：租户管理菜单不显示

**原因：** 前端没有正确读取用户角色

**解决：**
1. 清除浏览器缓存
2. 重新登录
3. 检查控制台是否有错误

### 问题 3：500 错误 "relation management.tenants does not exist"

**原因：** 没有执行配置脚本

**解决：**
```bash
psql -U postgres -d onebase -f scripts/setup_multi_tenant.sql
```

## 📚 下一步开发

- [ ] 租户用户邀请功能
- [ ] 租户内角色细分
- [ ] 数据隔离策略
- [ ] 租户配额管理
- [ ] 审计日志查看界面
- [ ] 租户统计dashboard

## 🤝 技术支持

如有问题，请查看：
- 后端日志：`cargo run` 输出
- 前端控制台：浏览器开发者工具
- 数据库日志：PostgreSQL 日志文件

---

**配置完成后，您将拥有一个完整的多租户数据库管理平台！** 🎉

